export const otherAngle = (firstAngle: number, secondAngle: number): number => {
  let sumaTotal: number = 180;
  return sumaTotal - firstAngle - secondAngle;
};

// export const otherAngle = (firstAngle: number, secondAngle: number): number => {
//   let sumaTotal: number = 180;
//   let thirdAngle: number = sumaTotal - firstAngle - secondAngle;
//   return thirdAngle;
// };
