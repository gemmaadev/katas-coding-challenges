export const makeNegative = (num: number): number => {
  return num > 0 ? -num : num;
};

// export const makeNegative = (num: number): number => {
//   if (num > 0) {
//     return -num;
//   }

//   return num;
// };
//